# ===========================================
# AssistMe Virtual Assistant - Environment Variables
# ===========================================

# =========== AI Provider Configuration ===========
# Choose ONE of the following options:

# Option 1: OpenRouter API (Recommended for free models)
# Get your key at https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_key_here

# Option 2: Vercel AI Gateway (Recommended for production)
# Create at https://vercel.com/dashboard → AI → Gateway
# AI_GATEWAY_API_KEY=your_ai_gateway_key_here

# Default model to use (Optional)
# OPENROUTER_DEFAULT_MODEL=meta-llama/llama-3.3-70b-instruct:free

# =========== Development Settings ===========
# Set to 'true' to use mock responses without an API key
DEV_MODE=false

# =========== Database Configuration ===========
# Defaults to SQLite for local development
# For production, use PostgreSQL:
# DATABASE_URL=postgresql+psycopg://user:pass@host:port/dbname

# =========== App Configuration ===========
# Used for CORS and API headers
APP_URL=http://localhost:5173

# Vercel-specific (auto-set by Vercel)
# VERCEL_URL=your-project.vercel.app

# Custom CORS origins (comma-separated)
# CORS_ALLOW_ORIGINS=https://example.com,https://app.example.com

# =========== Optional Analytics ===========
# API Analytics key for monitoring
# API_ANALYTICS_KEY=your_analytics_key

# =========== Security (Optional) ===========
# Shared secret for proxy requests
# CHAT_PROXY_SHARED_SECRET=your_secret_here
